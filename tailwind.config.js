/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './src/**/*.{js,jsx,ts,tsx}',  // Includes all files in the src folder (JavaScript, JSX, TypeScript, TSX)
    './public/index.html',         // Includes the main HTML file in the public folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
